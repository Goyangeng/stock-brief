spring:
  application:
    name: stock-brief

  datasource:
    url: jdbc:postgresql://localhost:5432/stockbrief
    username: stockbrief
    password: stockbrief

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
